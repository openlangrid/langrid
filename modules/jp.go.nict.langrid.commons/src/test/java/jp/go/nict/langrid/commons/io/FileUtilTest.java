package jp.go.nict.langrid.commons.io;


public class FileUtilTest {
/* temporary disabled
	@Test
	public void testIterateLines() throws Exception{
		try(CloseableIterator<String> it = FileUtil.iterateLines(new File("pom.xml"), "UTF-8")){
			Assert.assertTrue(it.hasNext());
			Assert.assertTrue(it.next().length() > 0);
		}
	}

	@Test
	public void testIterateLines2() throws Exception{
		try(CloseableIterator<String> it = FileUtil.iterateLines(new File("pom.xml"), "UTF-8")){
			while(it.hasNext()){
				it.next();
			}
		}
	}
*/
}
